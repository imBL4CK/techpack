[CFG=1.0.0]

/* Which of the built-in multiparts should be enabled? */
C "Vanilla Parts"={
	/* Should chains be a valid multipart target? */
	B "Chain"=true
	
	/* Should ladders be a valid multipart target? */
	B "Ladder"=true
	
	/* Should soul torches be a valid multipart target? */
	B "Soul Torch"=true
	
	/* Should torches be a valid multipart target? */
	B "Torch"=true
}